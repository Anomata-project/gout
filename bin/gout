#!/usr/bin/env python3
"""Run gout from a checkout: bin/gout directly, or through a symlink on PATH."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from gout.cli import main  # noqa: E402

sys.exit(main())
